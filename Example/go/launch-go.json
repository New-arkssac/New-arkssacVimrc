{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "go",
      "request": "attach",
      "name": "Attach debug",
      "program": "${workspaceFolder}",
      "cwd": "${workspaceFolder}",
      "mode": "remote",
      "remotePath": "${workspaceFolder}"
    }
  ]
}
